import sys
import os
import subprocess

if __name__ == '__main__':

	while(1):
		time
	file_name = 'log/test.log'
	cmd = "ls -l"
	#cmd = "tail -f +1 " + file_name
	subprocess.check_output(cmd.split())
	print(log_file)
#	log_file = os.system('tail -f +1 ' + file_name )
#	logs = log_file.split("\n")
#	for log in logs:
#		log_info = log.split(",")
#		print("log_info")
#		print(log_info)
